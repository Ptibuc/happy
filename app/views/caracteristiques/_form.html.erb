<%= form_for(@caracteristique) do |f| %>
  <% if @caracteristique.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@caracteristique.errors.count, "error") %> prohibited this caracteristique from being saved:</h2>

      <ul>
      <% @caracteristique.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :schema_id %><br>
    <%= f.text_field :schema_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

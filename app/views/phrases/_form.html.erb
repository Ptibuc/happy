<%= form_for(@phrase) do |f| %>
  <% if @phrase.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@phrase.errors.count, "error") %> prohibited this phrase from being saved:</h2>

      <ul>
      <% @phrase.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :condition %><br>
    <%= f.text_field :condition %>
  </div>
  <div class="field">
    <%= f.label :libelle %><br>
    <%= f.text_area :libelle %>
  </div>
  <div class="field">
    <%= f.label :caracteristique_id %><br>
    <%= f.text_field :caracteristique_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
